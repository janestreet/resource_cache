(library (
  (name        resource_cache)
  (public_name resource_cache)
  (preprocess (pps (ppx_jane -annotated-ignores -check-doc-comments ppxlib.runner)))
  (libraries (async core))))



(jbuild_version 1)
