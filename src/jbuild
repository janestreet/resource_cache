(library (
  (name        resource_cache)
  (public_name resource_cache)
  (preprocess (
    pps (
      ppx_jane
      -dated-deprecation
      -annotated-ignores
      -check-doc-comments ppxlib.runner)))
  (libraries (async_kernel async_rpc_kernel core_kernel))))



(jbuild_version 1)
